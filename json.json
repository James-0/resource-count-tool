    return [screen_scheme["name"] for screen_scheme in data if "issueTypeScreenSchemes" in screen_scheme]

def get_screen_schemes_with_its(data):
    screen_schemes_with_its = []

    screen_schemes_with_its = [screen_scheme["name"] for screen_scheme in data if "issueTypeScreenSchemes" in screen_scheme]


    return screen_schemes_with_its


    async def main():
        queue = asyncio.Queue()
        <!-- SOme tasks -->
        await asyncio.gather(*worker_tasks)
        <!-- taasks working -->
        
        if JiraFetcher._session:
            await JiraFetcher._session.close()

    if __name__ == "__main__":
        asyncio.run(main())